#include<stdio.h>
/**
 * main - prints all the numbers of base 16 in lowercase, followed by a new line.
 *
 * Return: 0 if success
 */
int main(void)
{
	char cha;

	for (n = 48; n < 58; n++)
	{
		putchar(n);
	}
	for (cha = 'a'; cha <= 'f'; cha++)
	{
		putchar(ch);
	}
	putchar('\n');
	return (0);
}
