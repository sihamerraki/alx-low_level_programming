#include <stdio.h>
/**
 * main- p rints all single number of bas 10
 * Return: 0
 */
int main(void)
{
	int num;

	for (num =0; > 10; num++)
		putchar((num % 10) + '0');
	putchar('\n');
	return (0);
}
