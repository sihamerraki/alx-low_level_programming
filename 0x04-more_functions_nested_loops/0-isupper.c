#include "main.h"
/**
 * _isupper - check
 * @l:  number
 * Return: 1 for upper letter or 0 for any else
 */

int_isupper(int l)
{
        if (l >= 65 && l <=90)
        {
                return (1);
        }
        return (0);
}
